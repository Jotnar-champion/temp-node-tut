setInterval(() => {
  console.log("hello ShitHeads!");
}, 2000);

//setInterval is a continuos loop printing whatevewr is inside in a particular time period, kind of like poller, it runs in increments
console.log("Last task");
